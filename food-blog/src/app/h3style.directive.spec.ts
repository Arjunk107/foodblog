import { H3styleDirective } from './h3style.directive';

describe('H3styleDirective', () => {
  it('should create an instance', () => {
    const directive = new H3styleDirective();
    expect(directive).toBeTruthy();
  });
});
