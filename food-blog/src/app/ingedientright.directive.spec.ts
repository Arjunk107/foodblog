import { IngedientrightDirective } from './ingedientright.directive';

describe('IngedientrightDirective', () => {
  it('should create an instance', () => {
    const directive = new IngedientrightDirective();
    expect(directive).toBeTruthy();
  });
});
