import { AuthorheadingDirective } from './authorheading.directive';

describe('AuthorheadingDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorheadingDirective();
    expect(directive).toBeTruthy();
  });
});
