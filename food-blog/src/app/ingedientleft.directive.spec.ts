import { IngedientleftDirective } from './ingedientleft.directive';

describe('IngedientleftDirective', () => {
  it('should create an instance', () => {
    const directive = new IngedientleftDirective();
    expect(directive).toBeTruthy();
  });
});
